# Lambda - shows only command status via symbol color
# cyan=success, red=error, green=vim-normal
format = "$character"
add_newline = false

[character]
success_symbol = "[λ](bold cyan) "
error_symbol = "[λ](bold red) "
vimcmd_symbol = "[λ](bold green) "
