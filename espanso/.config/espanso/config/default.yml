# yaml-language-server: $schema=https://raw.githubusercontent.com/espanso/espanso/dev/schemas/config.schema.json

# Keep defaults predictable and avoid accidental global toggles.
toggle_key: OFF
search_shortcut: ALT+SPACE
