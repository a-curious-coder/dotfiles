onboarding = false
# herdr configuration
# Place this file at ~/.config/herdr/config.toml

# Show first-run notification setup on startup.
# Missing also shows onboarding; set false after you've chosen.
# onboarding = true

[theme]
# Built-in themes: catppuccin, terminal, tokyo-night, dracula, nord,
#                  gruvbox, one-dark, solarized, kanagawa, rose-pine,
#                  vesper
# name = "catppuccin"

# Follow host terminal light/dark appearance and switch Herdr UI themes.
# Existing manual behavior is unchanged unless this is true.
# auto_switch = false
# dark_name = "catppuccin"
# light_name = "catppuccin-latte"

# Override individual color tokens on top of the base theme.
# Accepts: hex (#rrggbb), named colors, rgb(r,g,b), or panel_bg = "reset"
# name/panel_bg/accent/blue/yellow/green/red below are rewritten by
# `office-theme` (../bin/.local/bin/office-theme) to match nvim/ghostty/tmux.
name = "catppuccin"
auto_switch = false
[theme.custom]
panel_bg = "#16233A"
accent = "#4B8BBE"
blue = "#4B8BBE"
yellow = "#F6EB61"

[terminal]
# Executable used for new interactive panes.
# Empty means $SHELL, then /bin/sh.
# default_shell = ""

# Startup mode for new interactive pane shells: "auto", "login", or "non_login".
# "auto" uses login shells on macOS and keeps the current behavior elsewhere.
# shell_mode = "auto"

# CWD policy for new panes, tabs, and workspaces when no explicit --cwd is provided.
# Use "follow" to inherit the source pane/workspace, "home" for $HOME,
# "current" for Herdr's process directory, or a fixed path such as "~/Projects".
# new_cwd = "follow"

[update]
# Update channel used by background version checks and `herdr update`.
# Defaults to "stable" on Linux/macOS and "preview" on Windows.
# Set explicitly to choose stable releases or opt-in preview builds.
# channel = "stable"

# Check herdr.dev for new Herdr versions in the background.
# version_check = true

# Check herdr.dev for remote agent-detection manifest updates in the background.
# manifest_check = true

[keys]
# Matches the tmux prefix (see ../tmux/.tmux.conf). Requires stty -ixon in
# zshrc (added) so ctrl+s doesn't freeze the terminal outside tmux.
prefix = "ctrl+s"

# Prefix-mode actions
# help = "prefix+?"
# settings = "prefix+s"
detach = "prefix+d"      # tmux default detach key, easier to recall than prefix+q
reload_config = "prefix+r" # matches tmux's `bind r source-file`
resize_mode = "prefix+shift+r" # moved off prefix+r, which reload_config now owns
open_worktree = "prefix+shift+o" # open an existing managed worktree
# open_notification_target = "prefix+o"
workspace_picker = ["prefix+w", "alt+s"]  # alt+s matches tmux M-s choose-session
goto = ["prefix+g", "alt+w"]              # alt+w matches tmux M-w choose-tree
new_workspace = "prefix+shift+n"  # tmux has no dedicated new-session key (prefix+: new-session); this is herdr's own default
# new_worktree = "prefix+shift+g"
# open_worktree = ""    # optional, unset by default
# remove_worktree = ""  # optional, unset by default; opens confirmation
rename_workspace = "prefix+shift+w"  # tmux default is prefix+$ (rename-session); "dollar" key syntax unverified, using herdr's own default instead
close_workspace = "prefix+shift+d"   # tmux has no default kill-session key either; herdr's own default
previous_workspace = "prefix+p"  # dedicated space-hop, freed from previous_tab below which you said did nothing useful
next_workspace = "prefix+n"      # dedicated space-hop, freed from next_tab below which you said did nothing useful
previous_agent = "f9"   # no tmux equivalent; alt+p chord was unreliable per herdr's own docs, function keys are the documented reliable category
next_agent = "f10"      # no tmux equivalent; alt+n chord was unreliable per herdr's own docs, function keys are the documented reliable category
focus_agent = "prefix+alt+1..9"  # jump directly to the Nth agent row; herdr's own suggested default (untested — same alt-chord caveat as above, verify it fires)
# remote_image_paste = "ctrl+v" # only active in herdr --remote; empty disables raw-key image paste
new_tab = ["prefix+c", "f2"]              # f2 matches tmux -n F2 new-window
rename_tab = ["prefix+shift+t", "f8", "prefix+comma"]  # f8 matches tmux F8; prefix+, matches tmux's actual default rename-window key
# previous_tab = "" # freed prefix+p for previous_workspace above; use switch_tab (prefix+1..9) to jump tabs directly
# next_tab = ""     # freed prefix+n for next_workspace above; use switch_tab (prefix+1..9) to jump tabs directly
switch_tab = "prefix+1..9"  # matches tmux's default prefix+0-9 direct window switching
# switch_workspace = ""   # optional indexed binding, e.g. "prefix+shift+1..9"
close_tab = ["prefix+shift+x", "prefix+ampersand"]  # prefix+& matches tmux's own kill-window binding
# rename_pane = "prefix+shift+p"
# edit_scrollback = "prefix+e"
focus_pane_left = ["prefix+h", "prefix+left"]   # prefix+arrow matches tmux's built-in default pane-select
focus_pane_down = ["prefix+j", "prefix+down"]
focus_pane_up = ["prefix+k", "prefix+up"]
focus_pane_right = ["prefix+l", "prefix+right"]
# cycle_pane_next = "prefix+tab"
# cycle_pane_previous = "prefix+shift+tab"
# last_pane = ""          # optional, unset by default; bind e.g. "prefix+tab" for global back-and-forth
split_vertical = ["prefix+v", "f5", "prefix+percent"]   # f5 matches your F5; prefix+% is tmux's own built-in default
split_horizontal = ["prefix+minus", "f6", "prefix+quote"] # f6 matches your F6; prefix+" is tmux's own built-in default
close_pane = ["prefix+x", "f12"]          # f12 matches tmux -n F12 kill-pane
zoom = ["prefix+z", "f11"]                # f11 matches tmux -n F11 resize-pane -Z
# resize_mode = "prefix+r"
# toggle_sidebar = "prefix+b"

# Navigate-mode movement. These local shortcuts win while navigate mode is open.
# They are independent from focus_pane_*. Do not include prefix+, esc, enter, tab, or 1..9 here.
# navigate_workspace_up = "up"
# navigate_workspace_down = "down"
# navigate_pane_left = "h"      # left arrow always focuses the pane to the left
# navigate_pane_down = "j"
# navigate_pane_up = "k"
# navigate_pane_right = "l"     # right arrow always focuses the pane to the right

# Custom commands use the same binding syntax.
# type = "shell" runs detached in the background.
# type = "pane" opens a temporary pane and closes it when the command exits.
# type = "popup" opens a session-modal terminal without changing the tab layout.
# Popup width and height accept terminal cells or percentages such as "80%".
# On Windows, command strings run through cmd.exe /d /c.
# [[keys.command]]
# key = "prefix+alt+g"
# type = "popup"
# command = "lazygit"
# width = "80%"
# height = "80%"
[[keys.command]]
key = "prefix+i"
type = "plugin_action"
command = "flock.farm.open"
description = "Open Flock Farm"

# Matches tmux M-z/M-b/M-g scratch popups (see ../tmux/.tmux.conf).
[[keys.command]]
key = "alt+z"
type = "popup"
command = "exec \"${SHELL:-sh}\""
description = "scratch terminal"
width = "83%"
height = "75%"

[[keys.command]]
key = "alt+b"
type = "popup"
command = "btop"
description = "btop"
width = "95%"
height = "95%"

[[keys.command]]
key = "alt+g"
type = "popup"
command = "sh -c 'EDITOR=nvim lazygit'"
description = "lazygit"
width = "95%"
height = "95%"

# termscope: pick a file or URL visible in the focused pane (the tmux-thumbs job).
# README suggests ctrl+shift+a / ctrl+e; prefix-based instead so ctrl+e stays
# zsh's end-of-line and ctrl+shift chords aren't relied on.
[[keys.command]]
key = "prefix+f"
type = "plugin_action"
command = "termscope.open"
description = "visible-screen file picker"

[[keys.command]]
key = "prefix+u"
type = "plugin_action"
command = "termscope.open-links"
description = "visible-screen link picker"

# vim-herdr-navigation: Ctrl+h/j/k/l crosses between Neovim splits and herdr
# panes as one space. Bare ctrl (not prefix+) so it works without pressing
# prefix first, matching vim-tmux-navigator's feel. Editor side is wired in
# ../nvim/.config/nvim/lua/herdr-nav.lua (kept separate from the vendored
# editor/nvim.lua so it still falls back to nvim-tmux-navigation, not
# vim-tmux-navigator's :TmuxNavigate*, when outside herdr).
[[keys.command]]
key = "ctrl+h"
type = "plugin_action"
command = "vim-herdr-navigation.left"
description = "navigate left (vim/herdr)"

[[keys.command]]
key = "ctrl+j"
type = "plugin_action"
command = "vim-herdr-navigation.down"
description = "navigate down (vim/herdr)"

[[keys.command]]
key = "ctrl+k"
type = "plugin_action"
command = "vim-herdr-navigation.up"
description = "navigate up (vim/herdr)"

[[keys.command]]
key = "ctrl+l"
type = "plugin_action"
command = "vim-herdr-navigation.right"
description = "navigate right (vim/herdr)"

# herdr-keys: fuzzy keybinding cheatsheet, reads this file plus nvim/ghostty/
# tmux configs. prefix+y for a split pane, ctrl+alt+y for a full tab.
[[keys.command]]
key = "prefix+y"
type = "plugin_action"
command = "herdr-keys.open-keys"
description = "keybinding cheatsheet"

[[keys.command]]
key = "ctrl+alt+y"
type = "plugin_action"
command = "herdr-keys.open-keys-tab"
description = "keybinding cheatsheet (tab)"

# herdr-triage: ranks agent panes by who's been waiting longest. No default
# binding upstream; prefix+a ("attention") opens its live pane.
[[keys.command]]
key = "prefix+a"
type = "pane"
command = "herdr plugin pane open --plugin triage --entrypoint list --placement split"
description = "agent attention triage"

# iris: fuzzy cheatsheet of AI agent skills/rules, filtered to the
# focused pane's detected agent when herdr reports one (else shows all).
[[keys.command]]
key = "prefix+shift+k"
type = "plugin_action"
command = "cmc.iris.open"
description = "skills cheatsheet (Iris)"

# gh-pr: PR status badge lives in ui.sidebar.agents.rows below ($pr token).
# prefix+o opens the PR in your browser; prefix+shift+g forces a refresh
# (auto-refresh already runs on pane focus / worktree events, throttled to
# 30s, so this is only for "I just pushed, check now").
[[keys.command]]
key = "prefix+o"
type = "plugin_action"
command = "gh-pr.open-pr"
description = "open PR for focused pane"

[[keys.command]]
key = "prefix+shift+g"
type = "plugin_action"
command = "gh-pr.refresh"
description = "refresh PR status"

# command-palette: fzf-pick and run any plugin action. README suggests
# prefix+p, but that's already this config's previous_workspace.
[[keys.command]]
key = "prefix+shift+p"
type = "plugin_action"
command = "jt.command-palette.open"
description = "command palette (fzf)"

# compose: docker-compose status for the focused workspace. The ps popup
# already has start/stop/up/down/restart bound inside it, so one key is enough.
[[keys.command]]
key = "prefix+alt+d"
type = "plugin_action"
command = "compose.ps"
description = "docker compose status"

# cmc.plugins: manage installed plugins and browse the public registry
# (herdr.dev) in one popup — press tab inside it to switch views.
[[keys.command]]
key = "prefix+m"
type = "plugin_action"
command = "cmc.plugins.open"
description = "manage plugins"

# Legacy indexed shortcut config is still parsed for compatibility.
# Prefer switch_tab, switch_workspace, and focus_agent for new configs.
# [keys.indexed]
# tabs = ""       # e.g. "ctrl" makes ctrl+1..9 switch tabs directly
# workspaces = "" # e.g. "ctrl+shift" makes ctrl+shift+1..9 switch workspaces directly
# agents = ""     # e.g. "alt" makes alt+1..9 focus agent rows directly

# [worktrees]
# directory = "~/.herdr/worktrees"

[ui]
# Sidebar width (auto-scaled based on workspace names, this sets the default)
# sidebar_width = 26

# Minimum sidebar width when expanded (columns)
# sidebar_min_width = 18

# Maximum sidebar width when expanded (columns)
# sidebar_max_width = 36

# Start with the sidebar collapsed. Changes take effect on the next launch.
# sidebar_start_collapsed = false

# Collapsed sidebar presentation: "compact" keeps the narrow status rail, "hidden" uses zero width.
# sidebar_collapsed_mode = "compact"

# Terminal width at or below which Herdr uses the mobile single-column layout.
# Increase this for foldables, tablets, or wide phone terminals.
# mobile_width_threshold = 64

# Capture mouse input for Herdr's mouse UI.
# Set false to let the terminal handle normal clicks, such as Cmd-clicking URLs.
# Pane apps like lazygit and btop can still receive mouse when they request it.
mouse_capture = false

# Automatically copy text selected by mouse drag.
# Set false to keep drag selection visible without copying; double-click still copies a word.
# copy_on_select = true

# Host cursor policy: "auto", "native", or "drawn".
# "auto" draws Herdr's own cursor on native Windows builds and WSL to avoid ConPTY cursor flicker, and uses the native terminal cursor elsewhere.
# "native" always uses the outer terminal cursor. "drawn" always draws Herdr's cursor as terminal cell content.
# host_cursor = "auto"

# Optional modifier that forwards right-click hold/drag gestures to pane apps instead of opening Herdr's pane menu.
# Empty/off disables this. Shift is intentionally unsupported because terminals commonly reserve Shift+mouse.
# right_click_passthrough_modifier = ""

# Force a full redraw when the outer terminal regains focus.
# Set false to reduce visible flashing when switching back to Herdr.
# Trade-off: rare host terminal surface corruption may persist until the next full redraw.
# redraw_on_focus_gained = true

# Pane scrollback lines to scroll per mouse wheel notch.
# mouse_scroll_lines = 3

# Ask for confirmation before closing a workspace
# confirm_close = true

# Ask for a tab name before creating a new tab.
# Set false to create tabs immediately with generated names.
# prompt_new_tab_name = true

# Ask for a workspace name before interactive creation.
# prompt_new_workspace_name = false

# Draw borders around split panes.
# pane_borders = true

# Keep split panes visually separated instead of sharing divider borders.
# pane_gaps = true

# Show detected/reported agent labels in split pane borders when no manual pane name is set.
show_agent_labels_on_pane_borders = true

# Hide the tab row when a workspace has exactly one tab.
# New tabs can still be created with the configured keybinding.
# hide_tab_bar_when_single_tab = false

# Agent panel ordering: "spaces" (grouped by space) or "priority" (attention queue).
# "workspaces" is accepted as an alias for "spaces".
# agent_panel_sort = "spaces"

# Expanded agent rows. Built-ins are state_icon, state_text, workspace, tab, pane, agent,
# terminal_title, and terminal_title_stripped.
# Custom values reported through pane metadata use a $name token.
# A token occurrence may be styled with { token = "workspace", fg = "#89b4fa", bold = true, dim = false }.
# Omitted style fields preserve the contextual default.
# [ui.sidebar.agents]
# Blank rows between agent entries. Set to 1 to restore the previous spacing.
# row_gap = 0
# rows = [["state_icon", "workspace", "tab"], ["agent"]]
# Optional canonical agent IDs replace the default rows for matching agents.
# [ui.sidebar.agents.rows_by_agent]
# claude = [["state_icon", "workspace", "tab"], ["terminal_title_stripped"], ["agent"]]

# $pr adds the gh-pr plugin's PR status badge (#123 with a check/cross/dot icon).
# Second row uses terminal_title_stripped (not "agent") so multiple same-type
# agents (e.g. two Claude panes) in one workspace show their distinct task
# titles instead of all displaying the same generic "claude" label.
[ui.sidebar.agents]
rows = [["state_icon", "workspace", "$pr"], ["agent", "terminal_title_stripped"]]

# Expanded space rows. Built-ins are state_icon, state_text, workspace, branch, and git_status.
# Custom values reported through workspace metadata use a $name token, for example $jj_status.
# Inline token styles accept strict #RGB/#RRGGBB foregrounds plus bold and dim booleans.
# $ports: herdr-ports plugin (active dev server ports per space).
# $git_clean/$git_dirty/$git_conflict: ez-corp.git-status plugin (colored, ahead/behind aware;
# replaces the plain built-in git_status token — see plugins/config/ez-corp.git-status/config.toml).
# $claude_usage: claude-usage plugin (session/week quota %).
[ui.sidebar.spaces]
rows = [
  ["state_icon", "workspace", "$ports"],
  ["branch",
    { token = "$git_clean", fg = "#a6e3a1" },
    { token = "$git_dirty", fg = "#f9e2af" },
    { token = "$git_conflict", fg = "#f38ba8" }],
  ["$claude_usage"],
]

# Accent color for highlights, borders, and navigation UI.
# Accepts: hex (#89b4fa), named colors (cyan, blue, magenta), or rgb(r,g,b)
# accent = "cyan"

# Background notification popup delivery
[ui.toast]
# off = disable pop-up notifications
# herdr = show in-app toasts
# terminal = ask the outer terminal to show a desktop notification
# system = ask the OS notification service directly
delivery = "terminal" # surfaces through swaync via ghostty when herdr isn't focused
# delay_seconds = 1

[ui.toast.herdr]
# position = "bottom-right"

[ui.toast.clipboard]
# enabled = true
# position = "bottom-center"

# Play sounds when agents change state in background workspaces
[ui.sound]
# enabled = true
# Optional custom mp3 sound files. Relative paths are resolved from this config file's directory.
# path = "sounds/notification.mp3"   # one mp3 file for all sound notifications
# done_path = "sounds/done.mp3"      # overrides only finished notifications
# request_path = "sounds/request.mp3" # overrides only needs-attention notifications

# Per-agent overrides: default | on | off
# By default, droid is muted.
# [ui.sound.agents]
# droid = "off"

[session]
# Resume supported AI-agent panes into their native conversation sessions after
# a Herdr server restart. Requires official integrations that report session refs.
resume_agents_on_restore = true

[remote]
# Whether herdr manages the ssh config used for `herdr --remote`.
# When true (default), herdr runs remote ssh through a generated config that
# includes your ~/.ssh/config first and adds ServerAliveInterval/
# ServerAliveCountMax as fallbacks (so any keepalive values you set yourself
# still win) to survive idle network/NAT timeouts. Herdr also uses a private
# per-attach OpenSSH control socket to reuse the first authenticated connection.
# Set false to run plain ssh against your ssh config unchanged — this does not
# force keepalive or multiplexing off, it only stops herdr from adding its own.
# manage_ssh_config = true

[experimental]
# Allow launching herdr from inside a herdr-managed pane.
# allow_nested = false
# Experimental local Kitty graphics rendering for attached clients.
# Requires a Kitty graphics-compatible outer terminal.
# kitty_graphics = false
# Save recent pane screen history across full server restarts.
pane_history = true
# While prefix mode is active, temporarily switch the macOS host input
# source to an ASCII-capable keyboard layout so prefix commands register
# even when a CJK IME is active, then restore the previous input source
# when prefix mode exits. macOS only; best-effort. Default: false.
# switch_ascii_input_source_in_prefix = false
# Expose the focused pane's cursor to the outer terminal so macOS input
# methods keep tracking the candidate window when TUIs paint their own
# cursor (Claude Code, pi, codex). Trade-off: extra cursor visible for
# apps that hide it without painting a replacement (vim normal mode, etc.).
# reveal_hidden_cursor_for_cjk_ime = false
# Optional allow-list: only reveal for focused panes whose detected agent
# matches one of these names. Empty means apply to any focused pane.
# If the list contains no valid names, the reveal does not apply.
# Accepted: pi, claude, codex, gemini, cursor, devin, cline, opencode,
# copilot, kimi, kiro, droid, amp, grok, hermes, kilo, qodercli, qoder.
# cjk_ime_agents = []
# Cursor shape rendered when reveal_hidden_cursor_for_cjk_ime is true.
# Values: block, steady_block (default), underline, steady_underline, bar, steady_bar.
# cjk_ime_cursor_shape = "steady_block"

[advanced]
# Maximum scrollback buffer size in bytes retained per pane terminal.
# Matches Ghostty's default scrollback-limit behavior.
# scrollback_limit_bytes = 10000000
