# Hyprland QT app styling (hyprland-qt-support)
# Kepano: sharp corners, minimal borders, no animations

roundess = 0  # Sharp corners - consistent with window rounding

border_width = 0

reduce_motion = true  # Kepano: animations distract from content